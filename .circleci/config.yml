version: 2  
jobs:
  build:
    docker:
      - image: cimg/go:1.16
        auth:
          username: $dockeruser
          password: $dockerpas  # context / project UI env-var reference
    steps:
      - checkout
      # ... steps for building/testing app ...

      - setup_remote_docker:
          version: default
          docker_layer_caching: true
      # build and push Docker image
      - run: |
          
          docker build -t asyncio .