kind: ExternalService
name: simpleapps
spec:
    allow_http: true
    image: flaskapp:v1
    image_pull_policy: if_not_present
    image_pull_secret: dockerhub
    port: 5000
    replicas: 1
    resources:
        memory: 200Mi

